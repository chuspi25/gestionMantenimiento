# Configuración para pruebas
NODE_ENV=test

# Base de datos de prueba (PostgreSQL)
TEST_DB_HOST=localhost
TEST_DB_PORT=5432
TEST_DB_NAME=maintenance_test_db
TEST_DB_USER=postgres
TEST_DB_PASSWORD=rmzA-1

# JWT para pruebas
JWT_SECRET=test-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# Configuración de bcrypt para pruebas (menos rounds para velocidad)
BCRYPT_ROUNDS=4